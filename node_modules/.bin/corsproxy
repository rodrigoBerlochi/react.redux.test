#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../corsproxy/bin/corsproxy" "$@"
  ret=$?
else 
  node  "$basedir/../corsproxy/bin/corsproxy" "$@"
  ret=$?
fi
exit $ret
